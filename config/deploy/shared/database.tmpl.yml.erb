<%= fetch(:rails_env) %>:
  adapter: postgresql
  timeout: 5000
  encoding: utf8
  reconnect: true
  database: <%= "#{fetch(:application)}_#{fetch(:rails_env)}" %>
  pool: 75
  username: <%= "#{fetch(:application)}_#{fetch(:rails_env)}" %>
  password: 8LO9egzyxXBc*t
  host:
  port: 5432
